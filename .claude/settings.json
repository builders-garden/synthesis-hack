{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -Rr '.' | grep -q 'git commit' && bash conversations/sync.sh && git add conversations/ 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Syncing conversations..."
          }
        ]
      }
    ]
  }
}
