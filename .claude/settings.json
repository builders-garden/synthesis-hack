{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q '^git commit' && ./conversations/sync.sh && git add conversations/ 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Syncing conversations..."
          }
        ]
      }
    ]
  }
}
