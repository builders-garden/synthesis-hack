{
  "name": "@synthesis-hack/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "lint": "forge fmt --check",
    "deploy": "forge script script/Deploy.s.sol --rpc-url $BASE_RPC_URL --broadcast --verify"
  }
}
